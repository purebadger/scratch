// This flow supports selecting a patient in cosmic from anywhere you might find a CPR number (could be a spread sheet)

var cpr = Inputs['cpr'];

Wait.forField(Fields['Patientlinje - CPR'], 60);
Fields["Ryd CPR"].click();
if (cpr !== "-") {
  Fields["Patientlinje - CPR"].input(cpr);
  Fields["PatientSøg"].click();
}
